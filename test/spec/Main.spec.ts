/// <reference path="../references.ts" />

import {Main} from '../../src/Main';

describe('Main', () => {
  let main;

  beforeEach(() => {
    main = new Main();
  });

  describe('during initialization', () => {
    it('should log', () => {
      // given

      // when

      // then
    });
  });

});
